---
- name: Prepare
  hosts: all
  become: true
  tasks:
    - name: Ensure temporary directory exists
      ansible.builtin.file:
        path: /tmp/ansible
        state: directory
        mode: '0755'
